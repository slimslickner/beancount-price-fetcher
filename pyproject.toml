[project]
name = "beancount-price-fetcher"
version = "0.1.0"
description = "An opinionated price fetcher for beancount to be implemented as part of an automated import process."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
